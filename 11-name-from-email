public String nameFromEmail(String email) {
    //code here
    List<string> str=email.split('.');
    system.debug(str);
    string result=str[0]+' ';
    result+=str[1].substringBetween('.','@');
    return result;
}